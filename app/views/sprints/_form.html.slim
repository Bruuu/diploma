= form_for [@project, @sprint], html: { class: %w(form-horizontal) } do |f|
  - if @sprint.errors.any?
    - @sprint.errors.full_messages.each do |msg|
      .alert.alert-error
        a.close data-dismiss="alert" href="#" &times;
        = msg
  .control-group
    = f.label :name, class: %w(control-label)
    .controls = f.text_field :name
  .control-group
    = f.label :start_date, class: %w(control-label)
    .controls = f.text_field :start_date, class: %w(mykal)
  .control-group
    = f.label :end_date, class: %w(control-label)
    .controls = f.text_field :end_date, class: %w(mykal)
  .form-actions = f.submit class: %w(btn btn-primary)
